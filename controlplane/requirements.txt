fastapi>=0.104,<1
uvicorn[standard]>=0.24,<1
pydantic>=2.5,<3
pyyaml>=6.0,<7
numpy>=1.26,<3
pytest>=7.4,<9
httpx>=0.25,<1
presidio-analyzer>=2.2,<3
presidio-anonymizer>=2.2,<3
scikit-learn>=1.3,<2

# Real grounding: natural language inference against the retrieved source, and
# sentence embeddings for the counterfactual bias comparison. Both models are
# small and run on CPU. Set CONTROLPLANE_DISABLE_NLI=1 to force the lexical
# fallback on a machine with no model download.
sentence-transformers>=3.0,<4
torch>=2.0

# Optional. Only needed to point Screen 0 at a live model; with no API key
# configured the demo replays recorded responses and says so.
openai>=1.40,<2

# Reads .env.local so configuration does not have to live in shell exports.
# Optional: without it every setting falls back to the real environment.
python-dotenv>=1.0,<2
